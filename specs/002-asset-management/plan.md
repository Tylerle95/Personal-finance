# Implementation Plan: Asset & Account Management (with Custom Categories)

**Branch**: `002-asset-management` | **Date**: 2026-06-01 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/002-asset-management/spec.md`

---

## Summary

Build an asset holding management system that allows users to track their net worth across custom asset categories (such as Gold, Crypto, Bank Accounts, Stocks, Real Estate, etc.) that they create and manage. Users can create, update, and delete categories (with names, colors, and icons selected from predefined sets), assign asset accounts to these categories, view their total net worth, and see an allocation breakdown.

The implementation follows the Next.js 16 App Router + Supabase Server Actions pattern established in the authentication feature.

---

## Technical Context

**Language/Version**: TypeScript 5 / Next.js 16.2.6 / React 19

**Primary Dependencies**: `@supabase/ssr` (existing), `@supabase/supabase-js` (existing), `lucide-react` (existing), Tailwind CSS v4 (existing)

**Storage**: Supabase PostgreSQL — tables `asset_categories` (new) and `asset_accounts` (modified schema).

**Testing**: Manual verification via [quickstart.md](./quickstart.md)

**Target Platform**: Web PWA — Mobile-First, Standalone App mode

**Project Type**: Web application (Next.js App Router)

**Performance Goals**: Asset list, category list, and net worth dashboard render in < 1 second.

**Constraints**: Mobile-first layout, tap targets ≥ 48px, no horizontal scroll on mobile.

**Scale/Scope**: Single-user personal finance app.

---

## Constitution Check

*(Constitution template is empty — no blocking violations detected. Using project conventions from README.md and existing auth implementation as governance reference.)*

- ✅ Consistent pattern with existing auth (Server Actions + Server Components)
- ✅ RLS enforced for all new/modified tables
- ✅ Mobile-first design
- ✅ No new dependencies required

---

## Project Structure

### Documentation (this feature)

```text
specs/002-asset-management/
├── plan.md              ← This file
├── research.md          ← Phase 0 output (decisions + rationale)
├── data-model.md        ← Database schema, SQL migration
├── quickstart.md        ← Local verification steps
├── contracts/
│   └── interfaces.md    ← Server Action & data function contracts
└── tasks.md             ← Phase 2 output (generated by /speckit-tasks)
```

### Source Code (repository root)

```text
src/
├── app/
│   ├── actions/
│   │   └── assets.ts             # [MODIFY] Server Actions for Categories (create, update, delete) and Accounts (create, update, delete)
│   ├── dashboard/
│   │   ├── page.tsx              # [MODIFY] Fetch net worth summary and pass to DashboardClient
│   │   ├── DashboardClient.tsx   # [MODIFY] Adjust Net Worth summary and allocation chart grouping
│   │   └── assets/
│   │       ├── page.tsx          # [MODIFY] Page component fetching categories & accounts
│   │       └── AssetClient.tsx   # [MODIFY] Category management and Account CRUD UI (Tabs / Modals)
│   └── (layout/global)
├── components/
│   └── ui/
│       ├── AssetCard.tsx         # [MODIFY] Use category.icon & category.color instead of static config
│       └── AllocationChart.tsx   # [MODIFY] Group by custom categories
└── lib/
    ├── supabase/
    └── data/
        └── assets.ts             # [MODIFY] Database fetches: getUserAssetCategories, getUserAssetAccounts, getNetWorthSummary
```

---

## Design: Key Screens

### Screen 1: `/dashboard/assets` — Asset & Category List
- A main view displaying:
  - Header: "Tài sản của tôi" + "Thêm tài sản" button + "Quản lý danh mục" button.
  - If no categories exist, display: "Chưa có danh mục nào. Vui lòng thêm danh mục trước." with a primary CTA to create a category.
  - Accounts group layout: Listed under their respective parent categories (colored header with category icon + name).
  - Each account card: Displays its name, quantity, unit price, and total value (quantity × unit price). Has inline edit and delete actions.

### Screen 2: Manage Categories Modal/Tab
- Displays user's current categories.
- User can create a new category or edit/delete an existing one:
  - Fields: Name (text), Color (predefined palette swatch selector), Icon (predefined icon grid selector).
  - List of 8 predefined colors (Teal, Violet, Amber, Green, Blue, Rose, Indigo, Pink).
  - List of 12 finance/asset icons (Wallet, Landmark, Coins, TrendingUp, PiggyBank, CreditCard, Briefcase, Gem, Bitcoin, DollarSign, LineChart, Percent).

### Screen 3: Add / Edit Asset Modal
- Dropdown select for Category (fetching user's custom categories).
- Fields:
  - **Tên tài sản (Name)**: Text input (optional, defaults to the selected Category's name if left blank).
  - **Danh mục (Category)**: Select dropdown.
  - **Tiền tệ (Currency)**: Select dropdown supporting `VND` and `USD`.
  - **Ngày mua / ngày sở hữu (Purchase Date)**: Date input picker, defaulting to the current date.
  - **Dynamic Input Fields**:
    - If the selected Category's name contains cash/bank keywords (e.g. "Tiền mặt", "Ngân hàng", "Cash", "Bank", "Ví"), display a single **"Số dư / Giá trị"** (Balance / Value) number input. Under the hood, this sets `quantity = [Value]` and `unit_price = 1`.
    - Otherwise (e.g. for investments, crypto, gold), display separate **"Số lượng"** (Quantity) and **"Đơn giá"** (Unit Price) inputs.
- Live preview calculating: `Total value = Quantity × Unit Price` (or `Total value = Balance` for cash categories). If currency is `USD`, calculate the value in USD and display the estimated VND conversion using the fixed rate.

### Screen 4: Dashboard Net Worth Widget
- Total Net Worth value (sum of all asset accounts in VND).
- Donut or horizontal bar chart showing percentages of asset allocation grouped by custom Category.

---

## Key Constraints & Notes

1. **Decimal display**: Quantity decimals determined by type or category name hint, defaulting to standard locale rules. Support up to 8 decimal places for precise asset types (like crypto).
2. **Category Cascade**: Deleting a category will cascade delete all linked accounts. Warn the user before confirming category deletion.
3. **RLS**: Row-Level Security enabled on `asset_categories` and `asset_accounts` using `auth.uid() = user_id`. All queries in `src/lib/data/assets.ts` use the server-side Supabase client (`createClient()` from `src/lib/supabase/server.ts`) which automatically scopes queries to the authenticated user.
4. **Multi-Currency (USD/VND)**: The base currency for Net Worth calculation is VND. USD asset balances/valuations are converted to VND using a fixed exchange rate of **1 USD = 25,000 VND**.
5. **Purchase Date**: Every asset account record includes a `purchase_date` of type `DATE` representing the date of acquisition, defaulting to the current date.
6. **`updated_at` trigger**: Applied automatically via DB trigger; no application-layer logic needed.
